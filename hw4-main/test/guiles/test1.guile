(define (make-generator procedure)
  (define last-return values)
  (define last-value #f)
  (define (last-continuation _)
    (let ((result (procedure yield)))
      (last-return result)))
  (define (yield value)
    (call/cc (lambda (continuation)
               (set! last-value value)
               (last-return value))))
  (lambda args
    (call/cc (lambda (return)
               (set! last-return return)
                 (if (null? args)
                   (last-continuation last-value)
                   (apply last-continuation args))))))
(define (div x y) (if (and (exact-integer? x) (exact-integer? y)) (floor/ x y) (/ x y)))(define (pascal-display x)
  (cond ((and (not (exact-integer? x)) (real? x)) (display (exact->inexact x)))
        ((eq? x #t) (display 1))
        ((eq? x #f) (display 0))
        (#t (display x))))
(define main (lambda () (make-generator (lambda (return) (define a 0)(define b 0)(define c 0)(define u 0) (define d (make-array 0 '(1 10) )) (define e (make-array 0.0 '(1 10) )) (define g 0.0)(define h 0.0)(define x 0.0) (define k (make-array 0.0 '(23 57) '(23 57) )) (define l (make-array 0 '(23 57) '(23 57) ))  (define sort (lambda (a  ) (make-generator (lambda (return) (define b 0) (begin (set! c b) ))))) (define addition (lambda (a b  ) (make-generator (lambda (return) (begin (return (+ a b)) ))))) (define ggyy (lambda (g h  ) (make-generator (lambda (return) (begin (return (+ g h)) )))))  (begin (set! c ((addition 8 8 ))) (pascal-display c) (newline) (set! c ((addition (- 8) (- 8) ))) (pascal-display c) (newline) (set! x ((ggyy (exact->inexact 3.3) (exact->inexact 5.5) ))) (pascal-display x) (newline) (set! b 0) (if (> b a) (begin (set! a 111)) (begin (set! a 222))) (pascal-display a) (newline) (if (< b a) (begin (set! a 111)) (begin (set! a 222))) (pascal-display a) (newline) (if (eq? b a) (begin (set! a 111)) (begin (set! a 222))) (pascal-display a) (newline) (if (>= b a) (begin (set! a 111)) (begin (set! a 222))) (pascal-display a) (newline) (if (<= b a) (begin (set! a 111)) (begin (set! a 222))) (pascal-display a) (newline) (if (not (eq? b 0)) (begin (set! a 111)) (begin (set! a 222))) (pascal-display a) (newline) (set! a 1) (letrec ([loop (lambda () (if (< a 5) (begin (begin (pascal-display a) (newline) (set! a (+ a 1)) ) (loop))))]) (loop)) (begin (set! b 1) (letrec ([loop (lambda () (if (< b 11) (begin (begin (array-cell-set! d (+ b (* b (+ b 3))) b ) (set! b (+ b 1)) ) (loop))))]) (loop)) ) (array-cell-set! e (exact->inexact 9.99) 3 ) (array-cell-set! k (exact->inexact 3.14) 25 26 ) (array-cell-set! l 88 28 26 ) (pascal-display a) (newline) (set! u (array-cell-ref d 5 )) (pascal-display u) (newline) (set! g (array-cell-ref e 3 )) (pascal-display g) (newline) (set! g (array-cell-ref k 25 26 )) (pascal-display g) (newline) (set! u (array-cell-ref l 28 26 )) (pascal-display u) (newline) (set! u (- (+ 9 9) 10)) (pascal-display u) (newline) (set! u (+ (- (+ (div (+ 9 9) 3) (* 5 2)) 1) u)) (pascal-display u) (newline) )))))

((main))